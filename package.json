{
  "name": "tan-and-cheek",
  "version": "1.0.0",
  "description": "Shopify Theme",
  "main": "index.js",
  "scripts": {
    "deploy": "theme deploy",
    "watch": "node-sass scss/custom.scss assets/custom.css --watch --output-style compressed",
    "start": "theme watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.14.1"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
